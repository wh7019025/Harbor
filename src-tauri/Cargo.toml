[package]
name = "harbor"
version = "0.0.0"
description = "Harbor desktop console"
authors = ["Harbor"]
edition = "2021"
default-run = "harbor"

[workspace]
members = [".", "crates/harbor_core"]
resolver = "2"

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "harbor"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }
sha2 = "0.10"

[dependencies]
harbor_core = { path = "crates/harbor_core" }
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
parking_lot = "0.12"
libc = "0.2"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time", "net", "sync", "signal"] }
semver = "1"
ureq = { version = "2", features = ["json"] }
nvml-wrapper = "0.12.1"
sha2 = "0.10"

[profile.release]
codegen-units = 1
lto = "thin"
opt-level = "s"
panic = "abort"
strip = "symbols"

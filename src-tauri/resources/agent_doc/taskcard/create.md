# 新建任务

1. TaskClick → `+`（tasks）
2. Folder 从下拉选择：`Root` 或某个 search path
   - Root → 写入 `~/.harbor/st_taskcfg/tasks/`
   - search path → 写入 `{path}/st_taskcfg/tasks/`（目录不存在会创建）
3. YAML 用 Monaco 编辑，保存后出现在对应分类下
